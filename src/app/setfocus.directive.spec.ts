import { SetfocusDirective } from './setfocus.directive';

describe('SetfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SetfocusDirective();
    expect(directive).toBeTruthy();
  });
});
